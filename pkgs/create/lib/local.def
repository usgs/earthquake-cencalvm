# -*- Makefile -*-
#
# ----------------------------------------------------------------------
#
#                           Brad T. Aagaard
#                        U.S. Geological Survey
#
# <LicenseText>
#
# ----------------------------------------------------------------------
#

PROJ_TMPDIR = $(BLD_TMPDIR)/$(PROJECT)/$(PACKAGE)/lib

PROJ_CLEAN += $(PROJ_SAR)

# PROJ4
PROJ_CXX_INCLUDES += $(PROJ4_INCDIR)
ifdef PROJ4_LIBDIR
PROJ_LCXX_FLAGS += -L$(PROJ4_LIBDIR)
endif
PROJ_LCXX_FLAGS += -lproj

# ETREE
PROJ_CXX_INCLUDES += $(ETREE_INCDIR)
ifdef ETREE_LIBDIR
PROJ_LCXX_FLAGS += -L$(ETREE_LIBDIR)
endif
PROJ_LCXX_FLAGS += -letree

# version
# $Id$

# End of file 
